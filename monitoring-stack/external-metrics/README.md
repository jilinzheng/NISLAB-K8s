# Personal Reference to Build and Push

1. `docker build -t jilinnn/external-metrics-server:latest .`
2. `docker login` (if not logged-in already)
3. `docker push jilinnn/external-metrics-server:latest`
